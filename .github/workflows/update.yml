name: Update Monthly Game Releases

on:
  schedule:
    - cron: '0 0 1 * *'  # Le 1er de chaque mois à minuit
  workflow_dispatch:     # Permet de déclencher manuellement

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run update script
        run: npm run update || echo "Le script a échoué, mais le workflow continue."

      - name: Commit and push changes
        if: success() || failure()  # Exécute même si le script échoue
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add data/
          git diff --quiet && git diff --staged --quiet || git commit -m "Update game releases for $(date +'%Y-%m')"
          git push
